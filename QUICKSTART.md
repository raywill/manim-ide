# 快速开始指南

## 🚀 5分钟上手Manim Visual Editor

### 步骤1：启动项目

在项目根目录运行：

```bash
python3 -m http.server 8000
```

### 步骤2：打开编辑器

在浏览器中访问：
```
http://localhost:8000
```

### 步骤3：创建你的第一个场景

#### 方法一：手动创建

1. **选择正方形工具**
   - 点击左侧工具箱中的 `⬜ 正方形` 按钮
   - 光标变为十字形

2. **绘制正方形**
   - 在画布上按住鼠标左键
   - 拖拽绘制正方形
   - 释放鼠标完成创建

3. **编辑属性**
   - 双击刚创建的正方形
   - 右侧属性面板打开
   - 修改颜色、大小等属性
   - 实时预览更改效果

4. **添加箭头**
   - 点击左侧 `→ 箭头` 按钮
   - 在画布上拖拽创建箭头
   - 起点到终点的方向

5. **导出Manim代码**
   - 点击顶部 `📤 导出Manim代码` 按钮
   - 复制生成的Python代码
   - 或点击 `💾 下载为.py文件`

#### 方法二：导入示例场景

1. **获取示例JSON**
   - 打开 `temp/sample_scene.json`
   - 或访问 `temp/test_demo.html` 复制示例

2. **导入场景**
   - 点击顶部 `📥 导入JSON` 按钮
   - 选择JSON文件
   - 场景自动加载到画布

### 步骤4：在Manim中渲染

1. **保存导出的代码**
   ```bash
   # 保存为 my_scene.py
   ```

2. **渲染场景**
   ```bash
   manim my_scene.py GeneratedScene -pql
   ```
   
   参数说明：
   - `-p`: 渲染完成后预览
   - `-q`: 质量设置（`l`=低，`m`=中，`h`=高）
   - `-l`: 低质量（快速预览）

## 📋 常用操作

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + Z` | 撤销 |
| `Ctrl/Cmd + Y` | 重做 |
| `Delete` | 删除选中元素 |
| `Esc` | 切换到选择模式 |

### 鼠标操作

| 操作 | 功能 |
|------|------|
| 单击 | 选中元素 |
| 双击 | 打开属性面板 |
| 拖拽 | 移动元素位置 |
| 画布拖拽 | 绘制新元素（需先选择工具） |

### 工具说明

| 工具 | 图标 | 说明 |
|------|------|------|
| 选择 | 👆 | 选择和移动元素 |
| 正方形 | ⬜ | 创建正方形 |
| 矩形 | ▭ | 创建矩形 |
| 箭头 | → | 创建箭头 |
| 线段 | — | 创建直线 |
| 曲线 | 〰️ | 创建贝塞尔曲线 |
| 坐标系 | 📐 | 创建坐标系 |

## 💡 使用技巧

### 技巧1：精确定位

- 画布中心是Manim坐标系的原点(0,0)
- 右下角显示实时Manim坐标
- 双击元素，在属性面板输入精确坐标

### 技巧2：颜色选择

编辑器支持Manim预定义颜色：
- `#3498db` → `BLUE`
- `#e74c3c` → `RED`
- `#2ecc71` → `GREEN`

也可以使用自定义十六进制颜色。

### 技巧3：快速复制元素

1. 选中元素
2. 导出为JSON
3. 手动复制JSON中的element对象
4. 修改坐标后重新导入

### 技巧4：隐藏辅助元素

- 双击元素打开属性面板
- 勾选"隐藏（不导出）"
- 该元素将不出现在导出的代码中
- 适合用于辅助线、参考点等

## 🎯 示例场景

### 示例1：函数图像

```python
# 创建坐标系
1. 添加坐标系 (x_range=[-3,3], y_range=[-3,3])

# 添加曲线表示函数
2. 添加曲线，调整控制点模拟 y=x²

# 添加标注
3. 添加箭头指向关键点
```

### 示例2：几何证明

```python
# 创建三角形
1. 用3条线段构建三角形

# 添加标注
2. 用箭头标注边长
3. 用文本（待实现）标注角度
```

### 示例3：向量演示

```python
# 添加坐标系
1. 创建坐标系作为参考

# 绘制向量
2. 用箭头表示向量
3. 调整起点和终点

# 向量运算
4. 绘制向量和与差
```

## 🐛 故障排除

### 问题1：画布空白

**解决方案：**
- 刷新页面
- 检查浏览器控制台错误
- 确保使用HTTP服务器访问（不要直接打开文件）

### 问题2：无法绘制形状

**解决方案：**
- 确认已选择形状工具（按钮应高亮）
- 点击 `Esc` 重置状态
- 刷新页面重试

### 问题3：属性面板无法打开

**解决方案：**
- 确保双击在元素上（不是空白处）
- 切换到选择模式（点击 👆 选择按钮）
- 先单击选中元素，再双击

### 问题4：导出的代码无法运行

**解决方案：**
- 检查Manim是否正确安装：`manim --version`
- 确保使用Manim Community Edition
- 检查生成的代码语法（应该是有效的Python）

## 📚 下一步

- 阅读完整 [README.md](README.md) 了解详细功能
- 查看 [tests/test_basic.html](tests/test_basic.html) 运行自动化测试
- 访问 [Manim文档](https://docs.manim.community/) 学习更多动画技巧
- 尝试开发自定义形状插件（参考 [js/plugins/](js/plugins/) 目录）

## ⭐ 反馈与贡献

遇到问题或有改进建议？
- 提交 Issue
- 发起 Pull Request
- 分享你的作品！

---

**祝你创作愉快！🎬✨**

